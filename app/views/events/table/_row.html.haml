%tr
  %td
    %h3= event.name
  %td.text-nowrap
    = link_to 'Show', event_path(event), class: 'btn btn-secondary'
    = link_to 'Edit', edit_event_path(event), class: 'btn btn-secondary'
    = link_to 'Ticket Options', event_ticket_options_path(event), class: 'btn btn-secondary'
