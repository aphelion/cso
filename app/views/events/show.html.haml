.content
  %h1= @event.name
  - if (ticket = ticket_for_event_for_current_user(@event))
    = link_to 'View Ticket', ticket_path(ticket), class: 'btn btn-secondary'
  - else
    %ul
      - @event.ticket_options.each do |ticket_option|
        %li
          = ticket_option.name
          = link_to 'Purchase', new_event_ticket_option_ticket_path(@event, ticket_option), class: 'btn btn-secondary'
